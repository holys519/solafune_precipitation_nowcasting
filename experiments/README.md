Experiment logs and outputs are stored here. Each experiment should create a subfolder, e.g. `experiments/run_01/` containing checkpoints and logs.

This is the legacy experiment directory. For new experiments, use:
- `g_experiments/` for GPU cluster runs
- `l_experiments/` for local development runs
